using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class NameIndex
{
    public enum Layers {
        Trap = 8,
        Player = 9,
        Terrain = 15,
        MovingPlatforms=16,
    }
}
